<div id="regEvents">
    <h1>{{userName}}'s Registered Events</h1>
    {{#each events}}
    <ul id="events">
            <li><a id="event" href="/events/{{_id}}">{{name}}</a></li>
    </ul>
    {{/each}}
</div>